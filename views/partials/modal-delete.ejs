<div
  id="delete-modal"
  class="fixed inset-0 bg-black bg-opacity-70 hidden items-center justify-center z-50"
  role="dialog"
  aria-modal="true"
>
  <div class="bg-[#222222] rounded-lg p-6 max-w-sm w-full text-white">
    <h3 class="text-xl font-semibold mb-4 text-yellow-400">Confirmar eliminación</h3>
    <p class="mb-4">
      ¿Estás seguro de eliminar <span id="delete-name" class="font-bold"></span>?
    </p>
    <div class="flex justify-end gap-4">
      <button
        id="cancel-delete"
        type="button"
        class="px-4 py-2 bg-gray-700 rounded hover:bg-gray-600 text-white"
      >
        Cancelar
      </button>
      <form id="delete-form" method="POST" action="">
        <input type="hidden" name="_method" value="DELETE" />
        <button
          type="submit"
          class="px-4 py-2 bg-red-600 rounded hover:bg-red-500 text-white font-semibold"
        >
          Eliminar
        </button>
      </form>
    </div>
  </div>
</div>
