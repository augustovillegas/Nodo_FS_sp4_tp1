<div class="flex gap-4">
  <a
    href="<%= cancelHref %>"
    class="px-6 py-3 rounded bg-gray-700 hover:bg-gray-600 transition text-white font-semibold"
  >
    Cancelar
  </a>
  <button
    type="submit"
    data-confirm="true"
    class="px-6 py-3 bg-yellow-400 hover:bg-yellow-500 transition rounded font-semibold text-black"
  >
    <%= submitLabel || 'Guardar' %>
  </button>
</div>